{
  "name": "ineil77-dev",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Personal academic website for Indraneil Paul — ineil77.dev",
  "scripts": {
    "parse:cv": "node scripts/parse-cv.mjs",
    "dev": "npm run parse:cv && astro dev",
    "start": "npm run parse:cv && astro dev",
    "build": "npm run parse:cv && astro build && node scripts/csp-hashes.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/newsreader": "^5.3.0",
    "@fontsource-variable/source-code-pro": "^5.3.0",
    "@fontsource-variable/source-sans-3": "^5.3.0",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
